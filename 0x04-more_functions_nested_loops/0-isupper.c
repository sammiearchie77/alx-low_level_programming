#include "main.h"
#include <stdio>

/**
 * main - check the code.
 *
 * Return: Always 0
 */
int main(void)
{
	char c;

	if (c > 1)
	{
		print("%c: %d\n", c, _isupper(c));
	}
	else
	{
		print("%c: %d\n", c, _isupper(c));
	}
}
